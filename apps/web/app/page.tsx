export default function AlertsHomePage() {
  return (
    <main className="shell">
      <section className="card" aria-labelledby="page-title">
        <p className="eyebrow">BharatStudio Alerts</p>
        <h1 id="page-title">Your stream. Your alerts. Your control.</h1>
        <p className="lede">
          The Alerts web foundation is ready. Account, tipping, overlay and queue
          surfaces will appear only after their reviewed API contracts are enabled.
        </p>
        <div className="status" role="status">
          <span className="dot" aria-hidden="true" />
          L03 foundation in progress
        </div>
      </section>
    </main>
  );
}

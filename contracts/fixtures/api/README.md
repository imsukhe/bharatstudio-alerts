# OpenAPI v1 API examples

These synthetic JSON examples exercise the response/request shapes added to
`contracts/openapi/v1.yaml` for L03. They are not provider payloads and contain
no real personal, payment or credential data. JSON Schema fixtures for
cross-language event consumers remain in the parent `fixtures/` directory.

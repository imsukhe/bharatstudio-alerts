{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bharatstudio.in/contracts/v1/public-sticker-selection.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "selectionId", "stickerId"],
  "properties": {
    "$schema": { "type": "string", "format": "uri" },
    "schemaVersion": { "const": "v1" },
    "selectionId": { "type": "string", "format": "uuid" },
    "stickerId": { "type": "string", "format": "uuid" }
  }
}
